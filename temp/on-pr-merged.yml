name: On PR merged

on:
  pull_request:
    types: [closed]

jobs:
  move-linked-issues:
    if: github.event.pull_request.merged == true
    uses: chTestORG/org-issues/.github/workflows/move-linked-issues-to-qa.yml@main
    secrets:
      ORG_PAT: ${{ secrets.ORG_PAT }}
